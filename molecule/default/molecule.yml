---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: instance
    image: debian:stable
provisioner:
  name: ansible
  lint:
    name: ansible-lint
  inventory:
    links:
      group_vars: group_vars_${TEST_GROUP:-default}
scenario:
  name: default
verifier:
  name: testinfra
  lint:
    name: flake8
